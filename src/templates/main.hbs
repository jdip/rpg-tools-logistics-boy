<section id="{{this.meta.name}}-main-content" class="{{this.meta.name}}-interface">
    <form class="{{this.meta.name}}-interface-form">
        {{>(this.partials.header)}}
        <main>
            {{>(this.contentPartial)}}
        </main>
        {{>(this.partials.footer)}}
    </form>
</section>
