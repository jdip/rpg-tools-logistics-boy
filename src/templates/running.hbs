<section id="{{this.meta.name}}-running-content" class="{{this.meta.name}}-interface">
    <form class="{{this.meta.name}}-interface-form">
        {{>(this.partials.header)}}
        <main>
            <h2>{{localize 'RTLB.Running.Title'}}</h2>
            <BR><BR>

        </main>
        {{>(this.partials.footer)}}
    </form>
</section>
